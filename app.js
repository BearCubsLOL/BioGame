const MainCharacter = docuemnt.querySelector('#mc');

function characterController() {
    document.addEventListener("keydown", (e) -> {
       if (key.event == 87) {
           console.log("HELLO WORLD")
       } 
    });
}

characterController()
